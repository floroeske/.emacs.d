(setq auto-mode-alist (append '(("\\.launch$" . xml-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.urdf$" . xml-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.xacro$" . xml-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.world$" . xml-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.cu$" . c++-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.ino$" . c++-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.FCMacro$" . python-mode))
                              auto-mode-alist))

(setq auto-mode-alist (append '(("\\.bb$" . bitbake-mode))
                              auto-mode-alist))
